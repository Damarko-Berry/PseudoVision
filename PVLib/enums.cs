namespace PVLib
{
    public enum ShowStatus { New, Ongoing, Complete }
    public enum Channel_Type { TV_Like, Binge_Like }
    public enum PlaylistFormat { m3u, pls }
    public enum SecurityLevel {None, Public_Only, AlwaysUseLogin, CustomSecurityPolicy}
    public enum Access {User, Modderator, Admin}
}